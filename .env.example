# Supabase — projeto couple-quiz (org Carlos)
# Copie para .env.local e preencha apos criar o projeto no Dashboard ou via MCP.

VITE_SUPABASE_URL=https://rjlbqgdmbszstezxvghn.supabase.co
VITE_SUPABASE_ANON_KEY=sua_anon_key_aqui

# Modo de desbloqueio:
# - supabase: usa horarios do banco (producao)
# - local: usa storySchedule em content.js (dev sem Supabase)
VITE_UNLOCK_MODE=local

# PIN admin: configurado no Supabase (app_settings), nao neste arquivo.
